name: Azure CAF Rover Action
branding:
    icon: 'cloud-lightning'  
    color: 'green'
inputs:
  rover-version: # Docker tag
    description: 'Tag of the rover'
    required: true
runs:
  using: 'docker'
  image: 'docker://aztfmod/roverdev:${{ inputs.rover-version }}'